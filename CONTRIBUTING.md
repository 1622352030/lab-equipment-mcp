# 贡献指南

感谢你改进 `lab-equipment-mcp`。本项目能够远程改变真实仪器状态，因此代码正确性、
可追溯性和设备安全与功能数量同等重要。

提交贡献即表示你同意遵守 [行为准则](CODE_OF_CONDUCT.md)、[安全策略](SECURITY.md)
和 [竞赛与学术使用声明](COMPETITION_USE.md)。

## 开始之前

1. 先搜索现有 Issue 和 Pull Request，避免重复工作。
2. 新设备支持建议先提交 `New device support` Issue，说明型号、手册、接口和可用实机条件。
3. 不要在 `main` 上直接开发。Fork 仓库或从最新 `main` 创建独立分支。
4. 不要提交厂商手册、驱动安装包、许可证文件、凭据、设备序列号、私人 IP/COM 编号、
   未脱敏截图或实验室私有数据。

## 分支和提交

推荐分支名：

```text
feat/<vendor>-<model>
fix/<short-description>
docs/<short-description>
```

保持提交聚焦。共享传输层重构、设备驱动、文档和实机验收记录应尽量拆分为可审阅的提交。
提交信息可使用中文或英文，但必须准确描述改动。

## 新增或扩展设备

设备贡献至少应包括：

- `DeviceProfile`，列出手册声明的全部 USBTMC、RS-232、LAN、GPIB 等接口；
- 型号专用驱动和带型号前缀的 MCP 工具；
- 对范围、身份、解析、错误路径和安全保护的单元测试；
- `docs/<vendor>/<MODEL>.md` 设备说明；
- README 支持表和工具列表更新；
- 清楚标注“已实现但未实测”“配置读回验证”“用户目视验证”或“自动闭环实测”。

不得仅凭连接器形状推断通信协议，也不得把 SCPI 写入成功描述为物理输出验证。

## 手册和证据

- 优先使用用户手册和编程手册，并在设备说明中记录名称、版本和相关页码。
- 不要把可能受版权限制的完整手册复制进仓库。
- 实机证据必须脱敏，不能包含序列号、私人网络信息或无关实验数据。
- 固件相关差异必须记录固件版本；不能查询时应明确说明。

## 安全要求

- 发现、连接和身份查询应保持只读。
- 输出开启、触发、校准、固件、恢复出厂、文件删除和安全擦除必须使用显式确认门。
- 状态修改应尽可能读回验证；无法读回时返回“未验证”，不得假定成功。
- 每次实机测试都要在 `finally` 路径中关闭输出、恢复安全状态并断开会话。
- 不得在他人程序可能占用仪器时运行实机测试。

## 测试

提交前运行：

```powershell
uv sync --extra dev
uv run --extra dev pytest
uv run --extra dev ruff check .
uv run python scripts/verify_mcp.py
uv run python scripts/verify_mcp.py --github
```

新增设备时还应覆盖适用的模拟测试。没有实机条件可以提交实现，但必须把接口标为未实测。

## Pull Request 清单

Pull Request 中应说明：

- 修改目标和范围；
- 使用的手册、版本和页码；
- 实现及实际测试的接口；
- 驱动、VISA Runtime、固件和验证日期；
- 执行的测试命令和结果；
- 是否改变真实设备状态，以及如何恢复；
- 未验证功能、残余风险和兼容性影响；
- 是否与课程、论文、评奖或竞赛有关，并按 `COMPETITION_USE.md` 披露来源。

维护者可以要求补充证据、拆分提交或降低未经验证的能力声明。

## 许可证与原创性

贡献内容将按仓库的 MIT License 发布。你必须有权提交该内容，并保留第三方版权和许可证
声明。不要提交从厂商软件、其他参赛作品或闭源项目复制而来的代码。
